{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run TaskFlow",
            "request": "launch",
            "mainClass": "logic.ApplicationStartup",
            "classPaths": [
                "${workspaceFolder}/out"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}