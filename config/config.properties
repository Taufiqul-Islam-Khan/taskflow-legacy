# TaskFlow Configuration
app.name=TaskFlow
app.version=1.0

# Storage mode: memory or file
storage.mode=memory

# File path used only when storage.mode=file
storage.filepath=tasks.dat

# Display settings
display.max_tasks=20
display.show_completed=true

# Debug
debug.enabled=false
debug.log_level=INFO
